<view class="news-card" bindtap="handleTap">
  <view class="cover-wrap" wx:if="{{cover}}">
    <safe-image className="cover-slot-img" src="{{cover || ''}}" mode="aspectFill" />
  </view>
  <view class="body">
    <view class="category">{{category}}</view>
    <text class="title">{{title}}</text>
    <view class="meta" wx:if="{{date}}">
      <text>{{date}}</text>
      <text wx:if="{{views}}">阅读 {{views}}</text>
    </view>
  </view>
</view>
